const styles = theme => {
    return (
        {
            cardActions : {
                display : "flex",
                alignItems : "center",
                justifyContent : "flex-end"
            }
        }
    )
}

export default styles